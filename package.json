{
  "name": "xml---json-server",
  "version": "1.0.0",
  "description": "working on API using JSON server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^4.6.2",
    "json-server": "^0.17.3"
  },
  "devDependencies": {
    "node-sass": "^8.0.0",
    "typescript": "^5.0.4"
  }
}
